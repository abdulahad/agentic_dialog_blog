Convert this plan to markdown, make an estimated length for each section and write it at the end of the title of each h2 section. There should be no text between the title and the first section; the total length of the document needs to be {text_length}. 
Do not write any text under any section of the plan, simply format the plan into markdown. Return the plan in markdown, and nothing else. Here is the plan:
{user_brief}